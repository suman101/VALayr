# Python requirements — pinned for determinism
# Do NOT use >= or ~= specifiers. Pin exact versions.

# No bittensor import in core — only in subnet-adapter wrapper
# bittensor==7.3.1  # Uncomment when integrating with Bittensor

# Core dependencies
pycryptodome==3.21.0
jsonschema==4.23.0

# Dev / test dependencies
pytest==8.3.4
pytest-cov==5.0.0
pytest-timeout==2.4.0
ruff==0.8.6
black==24.10.0
mypy==1.14.1
